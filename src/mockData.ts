import { UserProfile, Exam, StudyItem, Material, MistakeItem } from "./types";

export const initialUser: UserProfile = {
  id: "guest_user",
  name: "শিক্ষার্থী",
  email: "student@oddhoyon.app",
  grade: "HSC / College",
  language: "bn",
};

export const initialExams: Exam[] = [];

export const initialStudyItems: StudyItem[] = [];

export const initialMaterials: Material[] = [];

export const initialMistakes: MistakeItem[] = [];

